name: Docker Build and Run

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Build Docker image
      run: docker build -t amz .

    - name: Log in to Docker Hub
      run: echo "dckr_pat_M7LxUtwkxzD2KNo9BrIqAo1y7R0" | docker login -u rohith709370 --password-stdin

    - name: Push Docker image to Docker Hub
      run: docker push amz

  deploy:
    runs-on: ubuntu-latest

    needs: build

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Log in to Docker Hub
      run: echo "dckr_pat_M7LxUtwkxzD2KNo9BrIqAo1y7R0" | docker login -u rohith709370 --password-stdin

    - name: Pull Docker image from Docker Hub
      run: docker pull amz

    - name: Run Docker container
      run: docker run amz
